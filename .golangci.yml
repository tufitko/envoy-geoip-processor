version: "2"

linters:
  default: standard
  exclusions:
    presets:
      - comments
      - common-false-positives
      - std-error-handling
    rules:
      # Unchecked writes in httptest handlers and test helpers are noise.
      - path: _test\.go
        linters:
          - errcheck
